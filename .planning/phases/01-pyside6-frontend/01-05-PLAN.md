---
phase: 01-pyside6-frontend
plan: 05
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
files_modified:
  - tests/conftest.py
  - tests/test_pyside6_protocol.py
  - tests/test_pyside6_structure.py
  - tests/test_pyside6_seat_map.py
  - tests/test_pyside6_event_log.py
  - tests/test_expiration_race.py
  - tests/test_cancel_race.py
  - tests/test_session_persistence.py
  - tests/test_reserve_consistency.py
autonomous: true
requirements:
  - FRNT-01
  - FRNT-02
  - VIS-01
  - VIS-02
  - EXPR-01
  - EXPR-02
  - CANC-01
  - SESS-01
  - SESS-02
  - RSRV-01
  - LOG-01
  - LOG-02

must_haves:
  truths:
    - "Every requirement (13 total) has at least one automated pytest test"
    - "conftest.py provides reusable fixtures: running ConcertServer, ConcertClient factory, seat matrix helpers"
    - "Concurrency tests use threading.Barrier to reproduce Prueba 3 (TTL expiration), Prueba 6 (cancel race), and Prueba 8 (session persistence)"
    - "All tests pass with `pytest tests/ -x -q`"
  artifacts:
    - path: "tests/conftest.py"
      provides: "Shared fixtures — server lifecycle, client factory, seat matrix helpers"
      contains: "@pytest.fixture"
    - path: "tests/test_pyside6_protocol.py"
      provides: "FRNT-01 verification — all protocol actions via PySide6 client"
      contains: "class TestPySide6ProtocolCompliance"
    - path: "tests/test_pyside6_structure.py"
      provides: "FRNT-02 verification — directory/module structure"
      contains: "def test_frontend_pyside6_exists"
    - path: "tests/test_pyside6_seat_map.py"
      provides: "VIS-01, VIS-02 verification — seat map visual states"
      contains: "class TestSeatMapWidget"
    - path: "tests/test_pyside6_event_log.py"
      provides: "LOG-01, LOG-02 verification — event log clarity and local/remote distinction"
      contains: "class TestEventLogWidget"
    - path: "tests/test_expiration_race.py"
      provides: "EXPR-01, EXPR-02 verification — TTL expiration under concurrent load"
      contains: "class TestExpirationRace"
    - path: "tests/test_cancel_race.py"
      provides: "CANC-01 verification — concurrent cancel idempotency"
      contains: "class TestCancelRace"
    - path: "tests/test_session_persistence.py"
      provides: "SESS-01, SESS-02 verification — disconnect/reconnect reclaim"
      contains: "class TestSessionPersistence"
    - path: "tests/test_reserve_consistency.py"
      provides: "RSRV-01 verification — single vs batch reserve consistency"
      contains: "class TestReserveConsistency"
  key_links:
    - from: "tests/conftest.py server fixture"
      to: "ConcertServer.start() / stop()"
      via: "pytest fixture with yield for setup/teardown"
      pattern: "@pytest.fixture.*def.*server"
    - from: "tests/test_cancel_race.py"
      to: "ConcertClient.cancel()"
      via: "threading.Barrier + concurrent threads"
      pattern: "threading.Barrier.*2"
    - from: "tests/test_expiration_race.py"
      to: "MonitorThread + ConcertClient.confirm()"
      via: "timed concurrency: create session with short TTL, run confirm during expiration window"
      pattern: "ttl.*sleep.*confirm"
---

<objective>
Create the test suite covering all 13 phase requirements with automated pytest tests. Build shared fixtures (`tests/conftest.py`), then create 8 test files covering protocol compliance, structure verification, seat map visuals, event logging, and four concurrency/integration scenarios (expiration race, cancel race, session persistence, reserve consistency).

Purpose: Validate that all teacher-flagged issues (Pruebas 3, 6, 8) are fixed, all visual requirements are met, and the PySide6 frontend correctly uses the JSON-over-TCP protocol.

Output: 9 test files (1 conftest + 8 test modules) with passing automated tests.
</objective>

<execution_context>
@/Users/quesadx/github/concert-sync/.opencode/gsd-core/workflows/execute-plan.md
@/Users/quesadx/github/concert-sync/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-pyside6-frontend/01-RESEARCH.md §Validation Architecture (lines 697-740 — test map with requirement → test type → automated command → file existence)
@.planning/phases/01-pyside6-frontend/01-PATTERNS.md lines 1042-1107 (test file patterns: imports, fixtures, class structure from existing TUI tests)
@tests/test_protocol_contract.py (existing protocol test — reuse RESERVE/CONFIRM/CANCEL patterns)
@tests/test_transaction_races.py (existing race test — reuse Barrier pattern)
@tests/test_transaction_idempotency.py (existing idempotency test — reuse pattern)
@tests/test_reserve_batch.py (existing batch test — reuse pattern)
@src/server/concert_server.py (ConcertServer start/stop lifecycle for conftest fixtures)
@src/client/concert_client.py (ConcertClient API for test requests)
@src/utils/enums.py (Section, SeatState, ReservationStatus enums)
@src/shared_resources/seat_matrix.py (SeatMatrix for state verification)
@frontend_pyside6/widgets/seat_map_widget.py (SeatMapWidget for widget tests)
@frontend_pyside6/widgets/event_log.py (EventLogWidget for log tests)
@frontend_pyside6/models/seat_state.py (SEAT_COLORS, CATEGORY_COLORS for assertions)
@pyproject.toml (pytest config: pythonpath = ["."])
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create conftest.py + Structure &amp; Protocol Tests (FRNT-01, FRNT-02)</name>
  <files>
    tests/conftest.py
    tests/test_pyside6_structure.py
    tests/test_pyside6_protocol.py
  </files>

  <read_first>
    @tests/test_protocol_contract.py (existing pattern: ConcertClient construction, send_request, response validation)
    @tests/test_tui_seat_map.py lines 11-22 (pytest fixture pattern with setup/teardown)
    @src/server/concert_server.py (ConcertServer.__init__, start, stop — lifecycle for fixtures)
    @src/client/concert_client.py (ConcertClient constructor: user_id, host, port)
    @src/utils/config.py (SERVER_PORT — default port for tests)
    @.planning/phases/01-pyside6-frontend/01-RESEARCH.md lines 729-739 (Wave 0 test gaps — what each test file must cover)
    @frontend_pyside6/__init__.py (verify import works)
    @frontend_pyside6/__main__.py (verify entry point structure)
    @frontend_pyside6/models/tracked_session.py (verify dataclass)
    @frontend_pyside6/models/seat_state.py (verify constants)
    @frontend_pyside6/widgets/__init__.py (verify barrel)
    @frontend_pyside6/workers/__init__.py (verify barrel)
  </read_first>

  <action>
    Create three test infrastructure files:

    ### 1. `tests/conftest.py` — Shared fixtures
    Create module-level fixtures following the project's existing pytest patterns:
    - `server()`: Starts `ConcertServer(port=9999)` on a background daemon thread, yields the server instance, stops server in teardown. Use `import threading`, `import time`, `from src.server.concert_server import ConcertServer`. Pattern: create server, call `server.start()`, wait 0.3s for port readiness, yield, `server.stop()` in finally.
    - `client(server)`: Creates `ConcertClient(user_id="test_user", host="localhost", port=9999)`, yields client, cleans up by sending CANCEL on any active transactions.
    - `client2(server)`: Creates a second `ConcertClient(user_id="test_user2", host="localhost", port=9999)` for multi-user tests, yields client2.
    - `seat_matrix(server)`: Returns `server.seat_matrix` for direct state access in tests.
    - `qapp()`: Creates `QApplication.instance()` or new `QApplication(sys.argv)` for widget tests. Use try/except for `RuntimeError` (QApplication already exists).

    Use `import pytest` and `@pytest.fixture(scope="function")` for all fixtures. Add `import sys` and `import os` at module top.

    ### 2. `tests/test_pyside6_structure.py` — FRNT-02
    Tests that verify the frontend package structure exists:
    - `test_frontend_pyside6_package_imports`: `import frontend_pyside6` succeeds
    - `test_models_module_imports`: `from frontend_pyside6.models.tracked_session import TrackedSession` succeeds
    - `test_widgets_module_structure`: `frontend_pyside6/widgets/` directory has `__init__.py`, `seat_map_widget.py`, `section_stats.py`, `transaction_panel.py`, `connection_panel.py`, `event_log.py` (check via `importlib.util.find_spec` or `os.path.exists`)
    - `test_workers_module_structure`: `frontend_pyside6/workers/` has `__init__.py`, `network_worker.py`
    - `test_frontend_tui_still_exists`: Verify `frontend_tui/__init__.py` still exists (FRNT-03 — TUI preserved)
    - `test_no_backend_imports_from_pyside6`: grep `src/` directory for any `import frontend_pyside6` or `from frontend_pyside6` — must return 0 matches (FRNT-03 — backend stable)

    ### 3. `tests/test_pyside6_protocol.py` — FRNT-01
    Tests that verify ConcertClient (used by PySide6) correctly performs all protocol actions. Use `client` fixture from conftest:
    - `test_reserve_seat_success(client)`: Reserve VIP(0,0), assert response status SUCCESS, tx_id present, ttl > 0
    - `test_confirm_success(client)`: Reserve → confirm, assert seat is SOLD via query_seat_map
    - `test_cancel_success(client)`: Reserve → cancel, assert seat is AVAILABLE
    - `test_query_returns_sections(client)`: query() returns sections dict with VIP/PREFERENTIAL/GENERAL keys
    - `test_query_seat_map_returns_grid(client)`: query_seat_map() returns seat_map dict with grid data
    - `test_reserve_batch_success(client)`: Reserve 2 seats in same section, assert both reserved
    - `test_invalid_section_returns_error(client)`: Reserve with section="INVALID", assert error

    Import `ConcertClientError` from `src.client.concert_client`. Use `pytest.raises(ConcertClientError)` for error cases.
  </action>

  <verify>
    <automated>pytest tests/test_pyside6_structure.py tests/test_pyside6_protocol.py -x -q --tb=short</automated>
  </verify>

  <acceptance_criteria>
    - `tests/conftest.py` has `server`, `client`, `client2`, `seat_matrix`, `qapp` fixtures, all `scope="function"`
    - `test_pyside6_structure.py` passes: all import checks succeed, `frontend_tui/` still exists, no backend imports from frontend_pyside6
    - `test_pyside6_protocol.py` passes: RESERVE, RESERVE_BATCH, CONFIRM, CANCEL, QUERY, QUERY_SEAT_MAP all return correct responses
    - `pytest tests/test_pyside6_structure.py tests/test_pyside6_protocol.py -x -q` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create Widget &amp; Event Log Tests (VIS-01, VIS-02, LOG-01, LOG-02)</name>
  <files>
    tests/test_pyside6_seat_map.py
    tests/test_pyside6_event_log.py
  </files>

  <read_first>
    @tests/test_tui_seat_map.py (existing pattern for widget tests: MagicMock, pytest fixtures, QApplication)
    @frontend_pyside6/widgets/seat_map_widget.py (SeatMapWidget API: __init__, update_grid, _resolve_display_state, seat_clicked signal)
    @frontend_pyside6/widgets/event_log.py (EventLogWidget API: append_event, append_line, CATEGORY_COLORS import)
    @frontend_pyside6/models/seat_state.py (SEAT_COLORS — for color assertions)
    @.planning/phases/01-pyside6-frontend/01-UI-SPEC.md §Seat Map State Colors, §Event Log Category Colors
    @.planning/phases/01-pyside6-frontend/01-PATTERNS.md lines 1042-1078 (test_seat_map_widget pattern)
  </read_first>

  <action>
    Create two widget test files:

    ### 1. `tests/test_pyside6_seat_map.py` — VIS-01, VIS-02
    Use `qapp` fixture from conftest. Create a fixture `vip_widget(qapp)` returning `SeatMapWidget("VIP", rows=5, cols=10)`.

    Test class `TestSeatMapWidget`:
    - `test_initial_grid_dimensions(vip_widget)`: rowCount=5, columnCount=10
    - `test_update_grid_applies_correct_colors(vip_widget)`: Create grid with AVAILABLE, RESERVED, SOLD states. Call `update_grid`. Assert item(0,0).background().color().name() matches SEAT_COLORS for each state.
    - `test_own_reserved_displays_blue(vip_widget)`: Grid with RESERVED at (0,0). Call `update_grid(grid, set(), {(0,0)})`. Assert item(0,0) background is OWN_RESERVED blue (#2196F3).
    - `test_pending_overlay_takes_priority(vip_widget)`: Grid with RESERVED at (0,0). Call `update_grid(grid, {(0,0)}, {(0,0)})`. Assert item(0,0) background is PENDING purple (pending_coords has highest priority over own_coords).
    - `test_sold_displays_red(vip_widget)`: Grid with SOLD at (2,2). Assert item(2,2) background is SOLD red (#F44336).
    - `test_click_emits_signal(qapp, vip_widget)`: Use `QSignalSpy` or manually connect to `seat_clicked`. Simulate by calling `vip_widget._on_cell_clicked(0, 0)` after setting up a grid with an item at (0,0). Verify signal emitted with correct section, row, col, state.

    Import `QColor` from `PySide6.QtGui`, `Qt` from `PySide6.QtCore`, `SEAT_COLORS` from `frontend_pyside6.models.seat_state`.

    ### 2. `tests/test_pyside6_event_log.py` — LOG-01, LOG-02
    Use `qapp` fixture from conftest. Create a fixture `log_widget(qapp)` returning `EventLogWidget()`.

    Test class `TestEventLogWidget`:
    - `test_append_event_inserts_html(log_widget)`: Call `append_event("LOCAL", "test message")`. Assert log_widget.toPlainText() or toHtml() contains "test message".
    - `test_local_event_has_green_color(log_widget)`: Call `append_event("LOCAL", "green msg")`. Assert toHtml() contains `color:#4CAF50` or `#4caf50`.
    - `test_remote_event_has_orange_color(log_widget)`: Call `append_event("REMOTE", "orange msg")`. Assert toHtml() contains `color:#FF9800`.
    - `test_error_event_has_red_color(log_widget)`: Call `append_event("ERROR", "error msg")`. Assert toHtml() contains `color:#F44336`.
    - `test_timestamp_included(log_widget)`: Call `append_event("SERVER", "ts msg")`. Assert toHtml() contains a timestamp pattern (digits + colon + digits).
    - `test_readonly(log_widget)`: Assert `log_widget.isReadOnly() == True`.

    Import `CATEGORY_COLORS` from `frontend_pyside6.models.seat_state`.
  </action>

  <verify>
    <automated>pytest tests/test_pyside6_seat_map.py tests/test_pyside6_event_log.py -x -q --tb=short</automated>
  </verify>

  <acceptance_criteria>
    - `test_pyside6_seat_map.py`: 6 tests pass — grid dimensions, correct colors for all states, OWN_RESERVED blue override, PENDING priority, SOLD red, click signal emission
    - `test_pyside6_event_log.py`: 6 tests pass — HTML insertion, LOCAL green, REMOTE orange, ERROR red, timestamp present, readonly
    - `pytest tests/test_pyside6_seat_map.py tests/test_pyside6_event_log.py -x -q` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create Concurrency &amp; Integration Tests (EXPR-01, EXPR-02, CANC-01, SESS-01, SESS-02, RSRV-01)</name>
  <files>
    tests/test_expiration_race.py
    tests/test_cancel_race.py
    tests/test_session_persistence.py
    tests/test_reserve_consistency.py
  </files>

  <read_first>
    @tests/test_transaction_races.py (existing concurrency test pattern: threading.Thread, threading.Barrier, server fixture, ConcertClient)
    @tests/test_transaction_idempotency.py (existing idempotency test pattern)
    @tests/test_reserve_batch.py (existing batch test pattern)
    @src/server/transactional_thread.py lines 472-529 (handle_cancel — idempotent after Plan 02 fix)
    @src/server/session_manager.py lines 30-73 (get_or_create, reclaim_session)
    @src/server/monitor_thread.py (MonitorThread — TTL sweep every 1s)
    @src/client/concert_client.py (full API)
    @.planning/phases/01-pyside6-frontend/01-RESEARCH.md lines 419-427 (EXPR-01 race description), lines 432-440 (CANC-01 race description), lines 330-343 (SESS-01/SESS-02 session persistence description), lines 452-461 (RSRV-01 RESERVE_SELECTED bug description)
  </read_first>

  <action>
    Create four concurrency/integration test files. Use `server`, `client`, `client2` fixtures from conftest.

    ### 1. `tests/test_expiration_race.py` — EXPR-01, EXPR-02
    Test class `TestExpirationRace`:
    - `test_concurrent_expire_and_confirm(server, client)`: Set up a session with short TTL (by creating a seat reservation), wait for TTL to pass, simultaneously run CONFIRM and let MonitorThread expire. Use `threading.Barrier(2)` to synchronize. After both complete, verify the seat is either SOLD (confirm won) or AVAILABLE (expire won), NEVER both. Verify semaphore counts are consistent with seat states.
    - `test_expire_during_transaction(server, client)`: Create reservation with TTL=1s via direct manipulation or by patching `RESERVATION_TTL`. Start confirm in a thread, sleep until TTL boundary, let MonitorThread sweep. Assert final state is consistent — no seats with invalid state.
    - `test_expiration_releases_semaphore(server, client)`: Reserve a seat, wait for TTL to expire (use short TTL via `RESERVATION_TTL` monkeypatch or mock), verify semaphore is released and seat is AVAILABLE after expiration sweep.

    Import `threading`, `time`, `ConcertClientError` from `src.client.concert_client`, `Section` from `src.utils.enums`, `RESERVATION_TTL` from `src.utils.config`.

    ### 2. `tests/test_cancel_race.py` — CANC-01
    Test class `TestCancelRace`:
    - `test_concurrent_cancel_idempotent(server, client)`: Reserve a seat to get a transaction_id. Create 2 threads with `threading.Barrier(2)` that both call `client.cancel(tx_id)` simultaneously. Assert both threads complete without raising `ConcertClientError`. After both complete, query server to verify seat is AVAILABLE (released once) and semaphore is released (once, not double-released).
    - `test_cancel_already_cancelled_returns_success(server, client)`: Reserve → cancel → cancel again. Assert second cancel returns status SUCCESS (not FAILURE per idempotency fix in Plan 02).

    ### 3. `tests/test_session_persistence.py` — SESS-01, SESS-02
    Test class `TestSessionPersistence`:
    - `test_reconnect_reclaims_seats(server, client, client2)`: Client1 reserves 2 seats → disconnect (create new client with same user_id but fresh ConcertClient). Create Client3 with different user_id → call `client.send_request({"action": "QUERY", "session_id": session_id})` or directly call `server.session_manager.reclaim_session(session_id, new_user_id)`. Verify reclaimed session has the 2 seats.
    - `test_session_id_reclaim_returns_existing_session(server)`: Directly test `SessionManager.reclaim_session()`: create session via `get_or_create("user1")`, reserve seats, then call `reclaim_session(session_id, "user2")`. Verify returned session has same seats and is mapped to "user2".
    - `test_expired_session_not_reclaimable(server, client)`: Create session, mark as expired (or wait TTL), attempt reclaim. Assert returns None.

    ### 4. `tests/test_reserve_consistency.py` — RSRV-01
    Test class `TestReserveConsistency`:
    - `test_reserve_selected_reserves_all_seats(server, client)`: Use `reserve_selected([{"section": "VIP", "row": 0, "col": 0}, {"section": "VIP", "row": 0, "col": 1}])`. After call, query_seat_map to verify BOTH seats are RESERVED, not just one. Check session.seats length is 2.
    - `test_reserve_batch_reserves_all_seats(server, client)`: Use RESERVE_BATCH (via send_request with action "RESERVE_BATCH" and seats list). Verify all seats reserved.
    - `test_single_vs_batch_consistency(server, client)`: Reserve 2 seats individually (2 RESERVE calls), note transaction_ids. Reserve 2 seats via batch, note transaction_id. Verify both methods result in correct seat states (seats RESERVED, semaphore counts correct).

    Import `ConcertClientError` from `src.client.concert_client`, `Section` from `src.utils.enums`, `SeatState` from `src.utils.enums`.
  </action>

  <verify>
    <automated>pytest tests/test_expiration_race.py tests/test_cancel_race.py tests/test_session_persistence.py tests/test_reserve_consistency.py -x -q --tb=short -p no:warnings</automated>
  </verify>

  <acceptance_criteria>
    - `test_expiration_race.py`: 3 tests pass — concurrent expire+confirm produces consistent state, no double release, semaphore matches
    - `test_cancel_race.py`: 2 tests pass — concurrent cancel is idempotent (both return SUCCESS), second cancel after first returns SUCCESS
    - `test_session_persistence.py`: 3 tests pass — reconnect reclaims seats, reclaim by session_id works, expired session is not reclaimable
    - `test_reserve_consistency.py`: 3 tests pass — reserve_selected reserves all seats, batch reserves all, single vs batch produce consistent results
    - `pytest tests/test_expiration_race.py tests/test_cancel_race.py tests/test_session_persistence.py tests/test_reserve_consistency.py -x -q` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → ConcertServer | Tests start/stop server on port 9999; no external network access |
| Test assertions → Shared state | Tests directly inspect SeatMatrix, SessionManager, SemaphoreManager state for correctness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | Port conflict during concurrent test runs | mitigate | Tests use function-scoped fixtures; `server` fixture binds to port 9999; `pytest -x` stops on first failure; no parallel test execution (`-p no:xdist`) |
| T-05-02 | Information Disclosure | Test logs may contain transaction IDs | accept | Transaction IDs are UUIDs, not PII; test output is local only |
</threat_model>

<verification>
1. Run all test files: `pytest tests/test_pyside6_*.py tests/test_expiration_race.py tests/test_cancel_race.py tests/test_session_persistence.py tests/test_reserve_consistency.py -x -q --tb=short`
2. Run existing test suite to verify no regressions: `pytest tests/ -x -q --tb=short`
3. Both must exit 0
</verification>

<success_criteria>
- 9 test files created: conftest.py + 8 test modules
- All 13 requirements mapped to at least one test
- Concurrency tests reproduce teacher-flagged Pruebas 3, 6, 8
- Existing test suite passes (no regressions from backend fixes in Plan 02)
- `pytest tests/ -x -q` exits 0
</success_criteria>

<output>
Create `.planning/phases/01-pyside6-frontend/01-05-SUMMARY.md` when done
</output>

## Artifacts This Plan Creates

| Symbol | Kind | File | Description |
|--------|------|------|-------------|
| `server` | fixture | tests/conftest.py | Starts ConcertServer on port 9999, yields, stops in teardown |
| `client` | fixture | tests/conftest.py | Creates ConcertClient("test_user", localhost, 9999), yields, cleans up |
| `client2` | fixture | tests/conftest.py | Creates ConcertClient("test_user2", localhost, 9999) for multi-user tests |
| `seat_matrix` | fixture | tests/conftest.py | Returns server.seat_matrix for direct state inspection |
| `qapp` | fixture | tests/conftest.py | Creates/returns QApplication singleton for widget tests |
| `TestPySide6ProtocolCompliance` | test class | tests/test_pyside6_protocol.py | 7 tests: RESERVE, CONFIRM, CANCEL, QUERY, QUERY_SEAT_MAP, RESERVE_BATCH, invalid section |
| `TestPySide6Structure` | test functions | tests/test_pyside6_structure.py | 6 tests: package imports, module structure, TUI preservation, no backend cross-imports |
| `TestSeatMapWidget` | test class | tests/test_pyside6_seat_map.py | 6 tests: dimensions, colors, OWN_RESERVED, PENDING priority, SOLD, click signal |
| `TestEventLogWidget` | test class | tests/test_pyside6_event_log.py | 6 tests: HTML insertion, LOCAL/REMOTE/ERROR colors, timestamp, readonly |
| `TestExpirationRace` | test class | tests/test_expiration_race.py | 3 tests: concurrent expire+confirm, expire during transaction, semaphore release |
| `TestCancelRace` | test class | tests/test_cancel_race.py | 2 tests: concurrent cancel idempotent, already-cancelled returns SUCCESS |
| `TestSessionPersistence` | test class | tests/test_session_persistence.py | 3 tests: reconnect reclaim seats, session_id reclaim, expired not reclaimable |
| `TestReserveConsistency` | test class | tests/test_reserve_consistency.py | 3 tests: reserve_selected all seats, batch all seats, single vs batch consistency |
