---
phase: 01-pyside6-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - flake.nix
  - frontend_pyside6/__init__.py
  - frontend_pyside6/__main__.py
  - frontend_pyside6/models/__init__.py
  - frontend_pyside6/models/tracked_session.py
  - frontend_pyside6/models/seat_state.py
  - frontend_pyside6/widgets/__init__.py
  - frontend_pyside6/workers/__init__.py
  - frontend_pyside6/resources/styles.qss
autonomous: false
requirements:
  - FRNT-02
user_setup:
  - service: pyside6
    why: "Package legitimacy audit requires human to verify PyPI listing before install"
    manual_verification: "Visit https://pypi.org/project/PySide6/ and confirm the package is maintained by 'Qt Project', has millions of downloads, LGPL license, and version >= 6.8"

must_haves:
  truths:
    - "PySide6 can be imported in Python 3.14 without ImportError"
    - "frontend_pyside6/ directory exists alongside frontend_tui/ with correct package structure"
    - "TrackedSession dataclass matches TUI semantics (identical fields, properties, and behavior)"
    - "pyproject.toml has separate pyside6 dependency group (parallel to tui group)"
  artifacts:
    - path: "pyproject.toml"
      provides: "pyside6 dependency group"
      contains: "[dependency-groups] pyside6"
    - path: "flake.nix"
      provides: "PySide6 in Nix dev shell"
      contains: "python314Packages.pyside6"
    - path: "frontend_pyside6/__init__.py"
      provides: "Package barrel"
      min_lines: 1
    - path: "frontend_pyside6/__main__.py"
      provides: "Entry point: QApplication + MainWindow"
      contains: "QApplication"
    - path: "frontend_pyside6/models/tracked_session.py"
      provides: "TrackedSession dataclass with TTL tracking"
      contains: "class TrackedSession"
    - path: "frontend_pyside6/models/seat_state.py"
      provides: "Visual state constants and color mapping"
      contains: "SEAT_COLORS"
    - path: "frontend_pyside6/resources/styles.qss"
      provides: "Qt stylesheet"
      contains: "QMainWindow"
  key_links:
    - from: "frontend_pyside6/__main__.py"
      to: "frontend_pyside6/main_window.py"
      via: "import ConcertMainWindow"
      pattern: "from frontend_pyside6.main_window import"
    - from: "frontend_pyside6/models/tracked_session.py"
      to: "frontend_tui/app.py TrackedSession"
      via: "identical dataclass fields and properties"
      pattern: "transaction_id.*operation_type.*seat_summary.*ttl_seconds.*created_at.*state"
---

<objective>
Establish the PySide6 environment and create the `frontend_pyside6/` package skeleton.

Purpose: Install PySide6 (the single new external dependency), update project configuration (pyproject.toml + flake.nix), and create the directory structure with all barrel files, models, and entry point so subsequent plans can build against known contracts.

Output: PySide6 installed and importable; `frontend_pyside6/` package with correct structure; TrackedSession and seat_state models defined; QSS stylesheet scaffolded.
</objective>

<execution_context>
@/Users/quesadx/github/concert-sync/.opencode/gsd-core/workflows/execute-plan.md
@/Users/quesadx/github/concert-sync/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-pyside6-frontend/01-RESEARCH.md
@.planning/phases/01-pyside6-frontend/01-PATTERNS.md
@.planning/phases/01-pyside6-frontend/01-UI-SPEC.md
@pyproject.toml
@flake.nix
@frontend_tui/app.py (lines 29-45 for TrackedSession blueprint)
@frontend_tui/__main__.py (for entry point pattern)
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify PySide6 Package Legitimacy (ASSUMED audit) then Install</name>
  <files>pyproject.toml, flake.nix</files>

  <read_first>
    @.planning/phases/01-pyside6-frontend/01-RESEARCH.md §Package Legitimacy Audit (lines 63-78)
    @pyproject.toml (current dependency-groups structure)
    @flake.nix (current packages list)
  </read_first>

  <action>
    RESEARCH.md §Package Legitimacy Audit tags `PySide6` as `[ASSUMED]` (slopcheck unavailable in this environment). Per protocol, a `checkpoint:human-verify` is required before installation.

    AFTER human approval:
    1. Run `uv add --group pyside6 "pyside6>=6.8"` to install PySide6 and update pyproject.toml with the `pyside6 = ["pyside6>=6.8"]` dependency group. This adds a new `[dependency-groups] pyside6` section after the existing `tui` group.
    2. Edit `flake.nix` at line 31 (after `python314Packages.textual`): add `python314Packages.pyside6` as a new line in the `packages` list. Indentation must match surrounding entries (12 spaces per the existing pattern in flake.nix lines 27-31).
    3. Verify: `python3 -c "from PySide6.QtWidgets import QApplication; print('PySide6 OK')"` exits 0.
  </action>

  <what-built>The agent will: (a) present the PyPI verification info from RESEARCH.md for your review, (b) after you approve, run `uv add --group pyside6 "pyside6>=6.8"` and edit `flake.nix` to add `python314Packages.pyside6`, (c) verify the import works.</what-built>
  <how-to-verify>
    1. Visit https://pypi.org/project/PySide6/
    2. Confirm: Maintainer = "Qt Project", Version >= 6.8 (6.11.1 as of May 2026), License = LGPL, Python 3.10-3.14 support, Millions of downloads
    3. Type "approved" to proceed with installation
  </how-to-verify>
  <resume-signal>Type "approved" to install PySide6, or describe any concerns</resume-signal>

  <verify>
    <automated>python3 -c "from PySide6.QtWidgets import QApplication; print('PySide6 OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `python3 -c "from PySide6.QtWidgets import QApplication; print('PySide6 OK')"` prints "PySide6 OK" and exits 0
    - `pyproject.toml` contains `[dependency-groups]` section with `pyside6 = ["pyside6>=6.8"]`
    - `flake.nix` contains `python314Packages.pyside6` on a line in the `packages` list
    - `uv.lock` includes pyside6 entry (regenerated by `uv add`)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create frontend_pyside6/ Package Skeleton</name>
  <files>
    frontend_pyside6/__init__.py
    frontend_pyside6/__main__.py
    frontend_pyside6/models/__init__.py
    frontend_pyside6/models/tracked_session.py
    frontend_pyside6/models/seat_state.py
    frontend_pyside6/widgets/__init__.py
    frontend_pyside6/workers/__init__.py
    frontend_pyside6/resources/styles.qss
  </files>

  <read_first>
    @frontend_tui/__init__.py (the 1-line docstring pattern: `"""Textual TUI frontend for ConcertSync."""`)
    @frontend_tui/__main__.py (entry point: `from frontend_tui.app import ConcertTextualApp` + `app.run()`)
    @frontend_tui/app.py lines 29-45 (TrackedSession dataclass: fields transaction_id, operation_type, seat_summary, ttl_seconds, created_at, state; properties expires_at, ttl_remaining)
    @.planning/phases/01-pyside6-frontend/01-UI-SPEC.md §Seat Map State Colors (AVAILABLE=#4CAF50, RESERVED_ME=#2196F3, RESERVED_OTHER=#FF9800, SOLD=#F44336, PENDING=#9C27B0) and §Event Log Category Colors
    @.planning/phases/01-pyside6-frontend/01-PATTERNS.md lines 49-55 (__init__.py), lines 60-80 (__main__.py), lines 690-717 (tracked_session.py), lines 721-752 (seat_state.py), lines 756-828 (styles.qss)
    @src/utils/enums.py (SeatState enum values — AVAILABLE, RESERVED, SOLD — to ensure seat_state.py uses compatible state strings)
  </read_first>

  <action>
    Create the `frontend_pyside6/` directory tree with these files. Follow the exact patterns documented in PATTERNS.md:

    1. **`frontend_pyside6/__init__.py`**: Module docstring `"""PySide6 desktop GUI frontend for ConcertSync."""` (mirrors `frontend_tui/__init__.py` pattern with adjusted frontend name).

     2. **`frontend_pyside6/__main__.py`**: Entry point that imports `ConcertMainWindow` from `frontend_pyside6.main_window` (forward reference — class not created until Plan 04). Per WARNING #2, wrap the import in try/except to handle the forward reference gracefully: `try: from frontend_pyside6.main_window import ConcertMainWindow` / `except ImportError: ConcertMainWindow = None`. The `main()` function checks `if ConcertMainWindow is None: print("ConcertMainWindow not yet implemented — complete Plan 04 first", file=sys.stderr); sys.exit(1)`. On success: `app = QApplication(sys.argv); window = ConcertMainWindow(); window.show(); sys.exit(app.exec())`. Include `if __name__ == "__main__": main()`. Follow PATTERNS.md lines 60-80 for the overall structure (imports, function layout, QApplication lifecycle).

    3. **`frontend_pyside6/models/tracked_session.py`**: Extract the `TrackedSession` dataclass verbatim from `frontend_tui/app.py` lines 29-45. Must have these EXACT fields: `transaction_id: str`, `operation_type: str`, `seat_summary: str`, `ttl_seconds: int`, `created_at: float`, `state: str = "ACTIVE"`. Must have these EXACT properties: `expires_at` (returns `self.created_at + self.ttl_seconds`), `ttl_remaining()` (returns `max(0, int(self.expires_at - time.time()))` if state=="ACTIVE", else 0). Use `from dataclasses import dataclass` and `import time`. Follow PATTERNS.md lines 694-717 exactly.

    4. **`frontend_pyside6/models/seat_state.py`**: Define visual state constants and QColor mapping. `VISUAL_STATES` tuple: `("AVAILABLE", "OWN_RESERVED", "RESERVED", "SOLD", "PENDING")`. `SEAT_COLORS` dict mapping each state to its QColor per UI-SPEC §Seat Map State Colors: AVAILABLE→#4CAF50, OWN_RESERVED→#2196F3, RESERVED→#FF9800, SOLD→#F44336, PENDING→#9C27B0. Also define `CATEGORY_COLORS` dict for event log per UI-SPEC §Event Log Category Colors: LOCAL→#4CAF50, REMOTE→#FF9800, ERROR→#F44336, SERVER→#9E9E9E, EXPIRE→#795548. Use `from PySide6.QtGui import QColor`. Follow PATTERNS.md lines 726-741 exactly.

    5. **`frontend_pyside6/widgets/__init__.py`** and **`frontend_pyside6/workers/__init__.py`**: Empty barrel files (same pattern as `frontend_tui/__init__.py` — just the module docstring or empty).

    6. **`frontend_pyside6/resources/styles.qss`**: Qt stylesheet adapting the TUI color palette. Copy the QSS from PATTERNS.md lines 763-828 exactly. This styles `QMainWindow` (bg #0f1722, text #e0f0ff), `QPushButton` (bg #1a2a3a, hover #2a3a4a, border #3b4a64), `QLineEdit` (bg #10171f), `QTableWidget` (bg #0b1220, gridline #2a3a4a, border #4f6f8f), `QHeaderView::section` (bg #1a2a3a, text #9ad4d6), `QTextEdit` (bg #0b1220), `QLabel` (text #d8e6f5), `QStatusBar` (bg #0f1722, text #9fb3c8).

    Create directories `frontend_pyside6/models/`, `frontend_pyside6/widgets/`, `frontend_pyside6/workers/`, `frontend_pyside6/resources/` first if they don't exist.
  </action>

  <verify>
    <automated>python3 -c "
from frontend_pyside6.models.tracked_session import TrackedSession
from frontend_pyside6.models.seat_state import SEAT_COLORS, CATEGORY_COLORS, VISUAL_STATES
s = TrackedSession('tx1', 'RESERVE', 'VIP(0,0)', 300, __import__('time').time())
assert s.transaction_id == 'tx1'
assert s.state == 'ACTIVE'
assert len(VISUAL_STATES) == 5
assert SEAT_COLORS['AVAILABLE'].name() == '#4caf50'
assert CATEGORY_COLORS['LOCAL'] == '#4CAF50'
print('All models OK')
"</automated>
  </verify>

  <acceptance_criteria>
    - `frontend_pyside6/__init__.py` exists with docstring containing "PySide6"
    - `frontend_pyside6/__main__.py` exists, imports `QApplication` and `ConcertMainWindow`, has `main()` with `app.exec()`
    - `frontend_pyside6/models/tracked_session.py` contains `class TrackedSession` with all 6 fields and `expires_at`/`ttl_remaining` properties
    - `frontend_pyside6/models/seat_state.py` contains `SEAT_COLORS` dict with 5 entries, `CATEGORY_COLORS` dict with 5 entries, `VISUAL_STATES` tuple
    - `frontend_pyside6/resources/styles.qss` contains `QMainWindow { background-color: #0f1722; }`
    - All barrel `__init__.py` files exist in models/, widgets/, workers/
    - `python3 -c "import frontend_pyside6"` succeeds
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Package manager (uv/pip) → PySide6 wheel | Untrusted package from PyPI — ASSUMED pending human verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `uv add pyside6` | mitigate | Task 1 is `checkpoint:human-verify` per Package Legitimacy Audit; human confirms PyPI listing before install |
| T-01-02 | Tampering | `styles.qss` loaded from file | mitigate | QSS is bundled in `frontend_pyside6/resources/`, not loaded from user-provided paths; stylesheet content is static and known |
| T-01-SC | Tampering | PySide6 pip install | mitigate | slopcheck unavailable — human checkpoint gates install; PySide6 is official Qt Project package with 8+ year history |

**Note:** TCP socket threats (plaintext, session hijacking) are evaluated in Plan 04 where network communication is implemented. This plan has no network surface.
</threat_model>

<verification>
Run the verifications from both tasks:
1. Task 1 checkpoint: human verifies PyPI listing, then agent verifies `python3 -c "from PySide6.QtWidgets import QApplication"`
2. Task 2 automated: `python3 -c "from frontend_pyside6.models.tracked_session import TrackedSession; ..."`
</verification>

<success_criteria>
- PySide6 importable in Python 3.14
- `frontend_pyside6/` has correct package structure with all barrel files
- TrackedSession dataclass is an exact semantic copy of the TUI original
- QSS stylesheet loaded with dark theme colors matching TUI palette
- pyproject.toml and flake.nix updated for PySide6 dependency
</success_criteria>

<output>
Create `.planning/phases/01-pyside6-frontend/01-01-SUMMARY.md` when done
</output>

## Artifacts This Plan Creates

| Symbol | Kind | File | Description |
|--------|------|------|-------------|
| `pyside6` dependency group | config | pyproject.toml | `[dependency-groups] pyside6 = ["pyside6>=6.8"]` |
| `python314Packages.pyside6` | config | flake.nix | Nix dev shell package entry |
| (barrel) | module | frontend_pyside6/__init__.py | Package docstring |
| `main()` | function | frontend_pyside6/__main__.py | Entry point: QApplication + MainWindow |
| `TrackedSession` | dataclass | frontend_pyside6/models/tracked_session.py | Fields: transaction_id, operation_type, seat_summary, ttl_seconds, created_at, state; properties: expires_at, ttl_remaining() |
| `VISUAL_STATES` | tuple | frontend_pyside6/models/seat_state.py | `("AVAILABLE", "OWN_RESERVED", "RESERVED", "SOLD", "PENDING")` |
| `SEAT_COLORS` | dict | frontend_pyside6/models/seat_state.py | State → QColor (5 entries) |
| `CATEGORY_COLORS` | dict | frontend_pyside6/models/seat_state.py | Category → hex color string (5 entries) |
| (barrel) | module | frontend_pyside6/widgets/__init__.py | Widgets package marker |
| (barrel) | module | frontend_pyside6/workers/__init__.py | Workers package marker |
| QSS stylesheet | resource | frontend_pyside6/resources/styles.qss | Dark theme: QMainWindow, QPushButton, QLineEdit, QTableWidget, QHeaderView, QTextEdit, QLabel, QStatusBar |
