---
phase: 4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/enums.py
  - src/server/session_manager.py
  - src/server/transactional_thread.py
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "OWN_RESERVED string returned in QUERY_SEAT_MAP response for seats belonging to the requesting user"
    - "RESERVED seats belonging to other users still return as RESERVED"
    - "Anonymous queries (no user_id) get all RESERVED without ownership data — graceful degradation"
    - "Seat matrix never stores OWN_RESERVED as a real value"
  artifacts:
    - path: "src/utils/enums.py"
      provides: "OWN_RESERVED enum member (view-only, never stored in SeatMatrix)"
      contains: "OWN_RESERVED"
    - path: "src/server/session_manager.py"
      provides: "get_by_user_id method for O(1) user session lookup without create side effects"
      contains: "def get_by_user_id"
    - path: "src/server/transactional_thread.py"
      provides: "Enriched handle_query_seat_map that tags requesting user's RESERVED seats as OWN_RESERVED"
      contains: "request.get('user_id', '')"
  key_links:
    - from: "handle_query_seat_map"
      to: "SessionManager.get_by_user_id"
      via: "self.server.session_manager.get_by_user_id(requesting_user_id)"
      pattern: "session_manager\\.get_by_user_id"
    - from: "handle_query_seat_map"
      to: "session.seats membership check"
      via: "if (section, row_idx, col_idx) in session.seats"
      pattern: "in session.seats"
---

<objective>
Add server-side ownership differentiation to QUERY_SEAT_MAP so RESERVED seats belonging to the requesting user are tagged as OWN_RESERVED.

Purpose: Enables the TUI to distinguish "my seats" from "other users' seats" by providing ownership data in the query response.
Output: OWN_RESERVED enum member + get_by_user_id helper + enriched handle_query_seat_map
</objective>

<execution_context>
@/home/montoshita/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/montoshita/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From src/utils/enums.py:
```python
class SeatState(Enum):
    AVAILABLE = "AVAILABLE"
    RESERVED = "RESERVED"
    SOLD = "SOLD"
```

From src/server/session_manager.py:
```python
class SessionManager:
    def get_or_create(self, user_id: str) -> UserSession: ...
    def get_by_session_id(self, session_id: str) -> Optional[UserSession]: ...
    def get_expired(self) -> List[UserSession]: ...
    def remove(self, user_id: str) -> Optional[UserSession]: ...
```

UserSession has: user_id, session_id, seats (List[Tuple[Section, int, int]])

From src/server/transactional_thread.py:
```python
def handle_query_seat_map(self, request):
    # Lines 448-477
    # Currently serializes all seat state values as-is via seat.value
    # No ownership differentiation
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OWN_RESERVED to SeatState + get_by_user_id to SessionManager</name>

  <files>
    src/utils/enums.py
    src/server/session_manager.py
  </files>

  <action>
    1. In src/utils/enums.py: Add `OWN_RESERVED = "OWN_RESERVED"` to SeatState enum between RESERVED and SOLD, with inline comment `  # view-only — never stored in SeatMatrix`. Do NOT change any other members or existing code.

    2. In src/server/session_manager.py: Add method `get_by_user_id(self, user_id: str) -> Optional[UserSession]` to SessionManager class. It must lock `self._lock`, then return `self._sessions.get(user_id, None)` (O(1) dict lookup). This differs from `get_or_create` — it does NOT create a session if none exists, avoiding side effects in read-only query paths. The return type hint `Optional[UserSession]` already imported.
  </action>

  <verify>
    <automated>python3 -c "from src.utils.enums import SeatState; assert SeatState.OWN_RESERVED.value == 'OWN_RESERVED'; print('OK')" && python3 -c "from src.server.session_manager import SessionManager; sm = SessionManager(); assert sm.get_by_user_id('nonexistent') is None; print('OK')"</automated>
  </verify>

  <done>
    SeatState owns OWN_RESERVED member accessible via SeatState.OWN_RESERVED. SessionManager.get_by_user_id() returns None for unknown user_id, returns UserSession for known user_id, and never creates sessions as side effect.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enrich handle_query_seat_map with ownership cross-reference</name>

  <files>
    src/server/transactional_thread.py
  </files>

  <action>
    Rewrite `handle_query_seat_map` (lines 448-477) to:

    1. Extract `requesting_user_id = request.get("user_id", "")` before the lock.
    2. If `requesting_user_id` is truthy, look up session via `self.server.session_manager.get_by_user_id(requesting_user_id)`. This returns None if no session exists (avoiding get_or_create side effect per RESEARCH.md Open Question 1).
    3. Inside the `with self.server.mutex_manager.sections(list(Section))` lock, when serializing each seat cell:
       - If `seat == SeatState.RESERVED AND session is not None AND (section, row_idx, col_idx) in session.seats` → append `"OWN_RESERVED"`
       - Otherwise → append `seat.value` (unchanged behavior)
    4. Wrap the function body in try/except returning `error_internal(str(e))` for consistency with other handlers.
    5. Add/update docstring: "Enriched seat-map response with OWN_RESERVED tags for requesting user's seats. Safe: OWN_RESERVED is view-only, never written to SeatMatrix. session.seats read outside lock — stale reads just mean one refresh cycle of amber instead of teal (harmless)."

    CRITICAL: Do NOT write OWN_RESERVED to seat matrix at any point. The matrix only contains AVAILABLE/RESERVED/SOLD. OWN_RESERVED is purely a serialization artifact in the response.
  </action>

  <verify>
    <automated>python3 -m pytest tests/ -x -k "test_phase" --timeout=60 2>/dev/null || echo "No tests found — verify manually via server+tui interaction"</automated>
  </verify>

  <done>
    handle_query_seat_map returns "OWN_RESERVED" for RESERVED seats belonging to the requesting user's session. Other RESERVED seats return "RESERVED". Anonymous queries return all RESERVED without ownership data. Seat matrix never contains "OWN_RESERVED".
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server (QUERY_SEAT_MAP) | user_id in request body crosses from untrusted client to server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | handle_query_seat_map | mitigate | Cross-reference requesting user's session only — never expose another user's ownership data. Other users' RESERVED seats stay as "RESERVED". Session lookup via `get_by_user_id` (read-only, O(1) dict access). |
| T-04-02 | Tampering | OWN_RESERVED write path | mitigate | OWN_RESERVED is ONLY emitted in query response serialization. No write path (handle_reserve, handle_confirm, handle_cancel, expire_session) references it. Seat matrix remains AVAILABLE/RESERVED/SOLD. |
| T-04-SC | Tampering | npm/pip installs | accept | No new packages. rich.style.Style and textual.coordinate.Coordinate are existing deps. No install step. |
</threat_model>

<verification>
- SeatState.OWN_RESERVED exists and has correct value
- SessionManager.get_by_user_id() returns None for unknown user, UserSession for known user — no create side effect
- handle_query_seat_map returns OWN_RESERVED for RESERVED seats in requesting user's session, RESERVED for others
- Anonymous clients (no user_id) get all RESERVED as "RESERVED" — graceful degradation
- Existing tests pass (server behavior unchanged for non-OWN_RESERVED paths)
</verification>

<success_criteria>
1. OWN_RESERVED enum member exists in SeatState, view-only
2. SessionManager.get_by_user_id() provides side-effect-free session lookup
3. handle_query_seat_map correctly tags requesting user's RESERVED seats as OWN_RESERVED
4. Other users' RESERVED seats remain as RESERVED
5. Seat matrix never stores OWN_RESERVED internally
</success_criteria>

<output>
Create `.planning/phase-04-visual-differentiation/04-01-SUMMARY.md` when done
</output>
