---
phase: 02-fix-expiration-restart-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/monitor_thread.py
  - src/server/concert_server.py
  - tests/test_phase2_e2e.py (NEW)
autonomous: true
requirements: [EXP-01, EXP-02, CLN-01, CLN-02, CLN-03]
user_setup: []
must_haves:
  truths:
    - "expire_reservation dead code is removed or fixed — seats can actually be released through the old path"
    - "Startup cleanup releases stale ReservationTable entries before listener accepts connections"
    - "Semaphores are released during cleanup (not just seat state)"
    - "All existing tests pass after changes"
  artifacts:
    - path: "src/server/monitor_thread.py"
      provides: "expire_reservation dead code removed or rewritten"
      min_lines: 60
    - path: "src/server/concert_server.py"
      provides: "_cleanup_stale_reservations() called in start() before listener thread"
      contains: "_cleanup_stale_reservations"
    - path: "tests/test_phase2_e2e.py"
      provides: "E2E tests for expiration fix and startup cleanup"
      min_lines: 80
  key_links:
    - from: "ConcertServer.start -> _cleanup_stale_reservations"
      to: "ReservationTable + SeatMatrix + SemaphoreManager"
      via: "Cleanup runs before listener start"
      pattern: "_cleanup_stale_reservations"
    - from: "MonitorThread.run"
      to: "SessionManager.get_expired + expire_session"
      via: "Session-based expiry (already correct, no changes)"
      pattern: "expire_session"
---

## Phase Goal

**As a** system operator, **I want to** fix broken expiration logic and ensure seats are never permanently blocked after a restart, **so that** the system is reliable under real-world uptime conditions.

<objective>
Fix the dead code in `expire_reservation()` that prevents seat release on expiration, and add startup cleanup to `ConcertServer` so stale ReservationTable entries are cleared on boot.

**Purpose:** After this plan, the old `expire_reservation()` code path is no longer broken (either removed or repaired), and a server restart automatically releases any seats that were left in RESERVED state from a prior crash.

**Output:** Fixed monitor_thread.py, startup cleanup in concert_server.py, and E2E tests for both.
</objective>

<execution_context>
@/home/montoshita/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/montoshita/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phase-02-fix-expiration-restart-cleanup/02-RESEARCH.md
@src/server/monitor_thread.py
@src/server/concert_server.py
@src/server/session_manager.py
@src/shared_resources/reservation_table.py
@src/shared_resources/seat_matrix.py
@src/shared_resources/semaphore_manager.py
@src/synchronization/mutex_manager.py
@src/utils/enums.py
@src/utils/config.py
@tests/test_phase1_e2e.py
@tests/test_reserve_batch.py
@tests/test_protocol_contract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix/remove expire_reservation dead code in monitor_thread.py</name>
  <files>src/server/monitor_thread.py</files>

  <read_first>
  @src/server/monitor_thread.py (full — focus on expire_reservation lines 64-93)
  @src/utils/enums.py (for ReservationStatus, SeatState)
  @.planning/phase-02-fix-expiration-restart-cleanup/02-RESEARCH.md (Code Examples section for approach options)
  </read_first>

  <action>
  Fix the dead code in `expire_reservation()`. The method currently:
  1. Returns early on line 71 (`if not reservation or ... return`) — lines 73-78 (seat release logic) are indented INSIDE the `if` block AFTER `return`, making them unreachable
  2. References `ordered_sections` and `seats_by_section` which are never defined in this method

  **Approach:** Remove the broken `expire_reservation()` method entirely. It is:
  - Deprecated since Phase 1 (no longer called from `run()`)
  - Based on the old ReservationTable model (not session-based)
  - Broken by design even if indentation were fixed (missing variables)

  Replace it with a lightweight safety wrapper:
  ```python
  def expire_reservation(self, tx_id):
      """Legacy safety wrapper — no longer called from run().

      Attempts to find session by transaction_id and expire it.
      If no matching session found, logs and returns.
      """
      for session in list(self.server.session_manager._sessions.values()):
          if session.session_id == tx_id:
              self.expire_session(session)
              return
      self.server.global_log.append(
          "EXPIRE",
          f"TX:{tx_id} not found in active sessions (already expired/confirmed)",
      )
  ```

  This preserves the public method signature (in case any external code references it) while making it functional. The actual expiration logic is entirely in `expire_session()`.

  The `run()` method and `expire_session()` remain UNCHANGED — they already work correctly.
  </action>

  <acceptance_criteria>
  1. `expire_reservation()` no longer has unreachable code after `return`
  2. `expire_reservation()` no longer references undefined `ordered_sections` or `seats_by_section`
  3. `expire_reservation()` no longer crashes when called
  4. `run()` still polls `session_manager.get_expired()` and calls `expire_session()` (unchanged)
  5. `expire_session()` logic is unchanged
  6. `python -c "from src.server.monitor_thread import MonitorThread; print('OK')"` passes
  </acceptance_criteria>

  <verify>
  <automated>python -c "from src.server.monitor_thread import MonitorThread; print('Import OK')"</automated>
  </verify>

  <done>Dead code fixed, expire_reservation no longer crashes, run() unchanged, all imports work.</done>
</task>

<task type="auto">
  <name>Task 2: Add startup cleanup to ConcertServer</name>
  <files>src/server/concert_server.py</files>

  <read_first>
  @src/server/concert_server.py (full — __init__, start(), stop())
  @src/shared_resources/reservation_table.py (full — structure of reservations dict, Reservation dataclass)
  @src/shared_resources/seat_matrix.py (full — SeatState values, set_seat_state, mutex_sections)
  @src/shared_resources/semaphore_manager.py (full — release_multiple)
  @src/utils/enums.py (for ReservationStatus, SeatState)
  @.planning/phase-02-fix-expiration-restart-cleanup/02-RESEARCH.md (Code Examples section for startup cleanup pattern)
  </read_first>

  <action>
  Add a `_cleanup_stale_reservations()` method to `ConcertServer` and call it in `start()` BEFORE `self.listener_thread.start()`.

  **A) Add `_cleanup_stale_reservations()` method:**
  - Iterate over `self.reservation_table.reservations`
  - For each reservation with `state == ACTIVE`:
    - Determine section and seats
    - For each seat tuple:
      - If 3-tuple `(section, row, col)`: use the embedded section
      - If 2-tuple `(row, col)`: use `reservation.section`
    - If seat is RESERVED in `seat_matrix`: mark AVAILABLE, count per section
    - Delete reservation from `reservation_table.reservations`
  - After all seats processed: release semaphores via `self.semaphore_mgr.release_multiple(section, count)` for each section with released seats
  - Log cleanup event to `global_log` with "CLEANUP" tag
  - Must handle the case where ReservationTable has mixed entry types (some from pre-session code stored as `(row,col)` tuples, newer ones stored differently)

  **B) Hook in `start()`:**
  - Add `self._cleanup_stale_reservations()` call:
    - After `self.monitor_thread.start()`
    - BEFORE `self.listener_thread.start()`
  - Rationale: MonitorThread starts polling immediately (harmless — no sessions exist yet on fresh boot). Cleanup runs next. Listener starts last — clients connect to a clean state.

  **C) Add import if needed:**
  - `from src.utils.enums import SeatState` if not already imported
  </action>

  <acceptance_criteria>
  1. `_cleanup_stale_reservations()` exists as a method on ConcertServer
  2. `start()` calls `_cleanup_stale_reservations()` before `listener_thread.start()`
  3. Stale ReservationTable entries are removed and seats marked AVAILABLE
  4. Semaphores are released for each section with released seats
  5. Empty ReservationTable produces no errors (graceful no-op)
  6. `python -c "from src.server.concert_server import ConcertServer; print('OK')"` passes
  </acceptance_criteria>

  <verify>
  <automated>python -c "from src.server.concert_server import ConcertServer; c = ConcertServer(port=19999); print('OK')"</automated>
  </verify>

  <done>Startup cleanup implemented, called before listener starts, handles stale entries, releases semaphores, no regressions.</done>
</task>

<task type="auto">
  <name>Task 3: Write E2E tests for Phase 2 expiration fix and startup cleanup</name>
  <files>tests/test_phase2_e2e.py (NEW)</files>

  <read_first>
  @tests/test_phase1_e2e.py (fixture pattern — ConcertServer on random port)
  @tests/test_reserve_batch.py (fixture pattern)
  @src/server/concert_server.py (to understand cleanup API and ReservationTable access)
  @src/shared_resources/reservation_table.py (for Reservation dataclass fields)
  @.planning/phase-02-fix-expiration-restart-cleanup/02-RESEARCH.md
  </read_first>

  <action>
  Create `tests/test_phase2_e2e.py` with E2E tests for the Phase 2 changes.

  Tests to write:

  **A) `test_expire_reservation_no_crash`** — Verify the fixed `expire_reservation()` doesn't crash:
  - Start server
  - Call server.monitor_thread.expire_reservation("nonexistent-tx-id")
  - No exception raised

  **B) `test_startup_cleanup_stale_reservations`** — Verify startup cleanup works:
  - Create server instance but don't start it yet
  - Manually inject a stale reservation into `server.reservation_table.reservations`:
    - Use `add_reservation()` method to create an ACTIVE reservation for VIP(0,0)
    - Mark the seat as RESERVED in seat_matrix directly
  - Call `server._cleanup_stale_reservations()` directly
  - Verify: seat is AVAILABLE, reservation removed from table
  - Use a test-dedicated port (not the default 9999)

  **C) `test_startup_cleanup_releases_semaphores`** — Verify semaphores released:
  - Same as B, but also verify that `semaphore_mgr.s_sections[section].acquire(blocking=False)` succeeds after cleanup (meaning the semaphore capacity was restored)

  **D) `test_expire_session_releases_seats`** — Verify session-based expiration still works:
  - Start server, create ConcertClient, reserve a seat (creates session)
  - Manually set session.last_activity far in the past (like time.time() - 600)
  - Call `server.monitor_thread.expire_session(session)`
  - Verify: seat is AVAILABLE, session removed from session_manager

  **E) `test_expire_reservation_finds_session_by_transaction_id`** — Verify the fixed wrapper:
  - Reserve a seat, get session_id as transaction_id
  - Call `server.monitor_thread.expire_reservation(session_id)` — should delegate to expire_session
  - Verify the session was expired and seat released

  Use `@pytest.fixture` pattern from test_phase1_e2e.py for server setup.
  All tests write to a random port to avoid conflicts.
  </action>

  <acceptance_criteria>
  1. `pytest tests/test_phase2_e2e.py --collect-only` shows 5 tests collected
  2. `test_expire_reservation_no_crash` passes (no exception)
  3. `test_startup_cleanup_stale_reservations` passes (stale reservation cleaned)
  4. `test_startup_cleanup_releases_semaphores` passes (semaphore released after cleanup)
  5. `test_expire_session_releases_seats` passes (session-based expiry works)
  6. `test_expire_reservation_finds_session_by_transaction_id` passes (delegation works)
  </acceptance_criteria>

  <verify>
  <automated>pytest tests/test_phase2_e2e.py -x -v 2>&1 | tail -20</automated>
  </verify>

  <done>5 new E2E tests cover expiration fix, startup cleanup, and session expiry. All pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| startup→network | Cleanup runs before listener starts — no external input during cleanup |
| MonitorThread→SeatMatrix | Expiration modifies seat state concurrently with transactional threads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | expire_reservation (dead code) | fix | Remove unreachable code. Replace with lightweight wrapper that delegates to expire_session. |
| T-02-02 | Tampering | _cleanup_stale_reservations → SeatMatrix | mitigate | Cleanup runs with ReservationTable lock held. Modifies seat state inside section mutex. No concurrent access because listener hasn't started. |
| T-02-03 | Denial of Service | Startup cleanup semaphore release | mitigate | For each section with released seats, call release_multiple. If cleanup over-releases, a future acquire could succeed when capacity is actually full. Double-check against actual seat state before releasing. |
| T-02-SC | Tampering | All code in this plan | mitigate | No external packages. Slopcheck to verify no code injection. |
</threat_model>

<verification>
1. `python -c "from src.server.monitor_thread import MonitorThread; from src.server.concert_server import ConcertServer; print('Imports OK')"` passes
2. `pytest tests/test_phase2_e2e.py -x -v` passes all 5 tests
3. `pytest tests/ -x --ignore=tests/concurrent_tests.py --ignore=tests/test_phase2_e2e.py` passes (all existing tests)
4. Manual: start server, reserve seat, wait for TTL expiry (or use short TTL), verify seat returns to AVAILABLE
</verification>

<success_criteria>
1. [ ] `expire_reservation()` no longer has unreachable code after `return`
2. [ ] `expire_reservation()` doesn't crash when called with valid/invalid tx_id
3. [ ] `_cleanup_stale_reservations()` runs before listener thread in `start()`
4. [ ] Stale reservations are removed and seats marked AVAILABLE on startup
5. [ ] Semaphores are released for cleaned-up seats
6. [ ] Session-based `expire_session()` still works correctly
7. [ ] All existing tests pass
8. [ ] 5 new E2E tests pass
</success_criteria>

<output>
Create `.planning/phase-02-fix-expiration-restart-cleanup/02-01-SUMMARY.md` when done
</output>
