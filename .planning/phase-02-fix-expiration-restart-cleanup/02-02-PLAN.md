---
phase: 02-fix-expiration-restart-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tests/concurrent_tests.py
  - tests/test_phase2_e2e.py
autonomous: true
requirements: [EXP-03]
user_setup: []
must_haves:
  truths:
    - "Expiration consistently releases all seats under concurrent RESERVE + expire race"
    - "Expiration releases all seats under concurrent RESERVE_BATCH + expire race"
    - "No false-positive verification: test asserts seats are actually AVAILABLE, not just that types check"
  artifacts:
    - path: "tests/concurrent_tests.py"
      provides: "Concurrent expiration stress test"
      contains: "test_expiration_releases_seats_under_concurrent_reserve"
    - path: "tests/test_phase2_e2e.py"
      provides: "Expiration under load — concurrent RESERVE + timer-based expiry"
      pattern: "concurrent_reserve_and_expire"
  key_links: []
---

<objective>
Verify that expiration works correctly under concurrent load — both the session-based `expire_session()` path and the overall system behavior when multiple threads are reserving seats while expiration fires.
</objective>

<execution_context>
@/home/montoshita/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/montoshita/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phase-02-fix-expiration-restart-cleanup/02-RESEARCH.md
@.planning/phase-02-fix-expiration-restart-cleanup/02-01-PLAN.md
@src/server/monitor_thread.py
@src/server/concert_server.py
@src/server/session_manager.py
@tests/concurrent_tests.py
@tests/test_phase2_e2e.py
@tests/test_phase1_e2e.py
@src/utils/enums.py
@src/shared_resources/seat_matrix.py
@src/shared_resources/semaphore_manager.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add concurrent expiration stress test to concurrent_tests.py</name>
  <files>tests/concurrent_tests.py</files>

  <read_first>
  @tests/concurrent_tests.py (full — understand ITERATIONS, THREADS_PER_SECTION, _check_invariants, existing test structure)
  @src/server/session_manager.py (for UserSession dataclass — last_activity manipulation)
  @tests/test_phase2_e2e.py (post-Plan-02-01 — for the server fixture pattern)
  @.planning/phase-02-fix-expiration-restart-cleanup/02-RESEARCH.md (for Pitfall 3: expire_session vs handle_confirm race)
  </read_first>

  <action>
  Add a concurrent stress test to `concurrent_tests.py` that verifies expiration releases all seats correctly under concurrent reserve and expire operations.

  **A) Add `test_expiration_releases_seats_under_concurrent_reserve()` function:**

  ```
  Setup:
    1. Start ConcertServer on random port
    2. Wait for server ready
    3. Create a list of ConcertClient instances (N=5), each with unique user_id
    4. Pre-compute seat assignments per user (each reserves 1-3 seats from different sections)

  Concurrent phase (ITERATIONS=20, THREADS=5):
    For each iteration i:
      - Thread t_i (user_id_i):
        a. RESERVE seats for this user
        b. Wait ~0.1s (simulates some hold time)
        c. Get the session_id from the reserve response
        d. Expire the session by calling the server's MonitorThread.expire_session
           (access via the test's reference to the ConcertServer instance)
    
    Alternative approach (simpler, doesn't need server ref in client threads):
      Instead of per-thread expire, use a global timer-based approach:
      - Set up a fixed short TTL (e.g., 2 seconds via config monkey-patch)
      - Start ITERATIONS threads reserving seats
      - Let MonitorThread expire sessions naturally after TTL
      - After all threads complete, verify all seats from expired sessions are AVAILABLE

  Verification:
    1. QUERY the server for section stats
    2. Assert that all seats are AVAILABLE (no RESERVED seats remain after expiry)
    3. Assert that seat counts match initial configuration (no seat loss)
    4. Assert semaphore counts are restored (all semaphores can be acquired)
  ```

  **Important implementation details:**
  - Use the existing `_wait_for_server()` helper to ensure server readiness
  - Use the existing `_check_invariants()` helper for final state verification
  - Set `RESERVATION_TTL` to 2 seconds for the test by importing the module and overriding `src.utils.config.RESERVATION_TTL` before server creation. Restore the original value in a `try/finally` or fixture teardown.
  - Use `threading.Barrier` to synchronize thread start (all threads start reserving simultaneously)
  - MonitorThread has 1-second polling interval — set TTL=2 ensures at least one poll cycle fires
  - Wait 4 seconds after all threads start before checking invariants (2x TTL + 1s polling jitter)

  **B) Register in the test runner:**
  - Add the test name to the conditional runner at the bottom of `concurrent_tests.py` if one exists
  - Ensure the test does NOT run in the default test suite (it takes 10-15 seconds)
  </action>

  <acceptance_criteria>
  1. `python -c "import tests.concurrent_tests"` imports without error
  2. Test creates ITERATIONS threads, all reserve seats, MonitorThread expires sessions
  3. After test completes, ALL seats are AVAILABLE (no leaked RESERVED seats)
  4. Semaphore counts match initial state
  5. Test takes < 20 seconds
  </acceptance_criteria>

  <verify>
  <automated>python -m pytest tests/concurrent_tests.py -x -v -k test_expiration_releases_seats 2>&1 | tail -15</automated>
  </verify>

  <done>Concurrent expiration stress test passes: seats released under concurrent load, semaphores restored, no leaked RESERVED seats.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Repudiation | Concurrent expiration test | mitigate | Test logs all reserve/expire actions. If seat leaks occur, log trace shows which sessions expired and which seats weren't released. |
| T-02-05 | Denial of Service | Semaphore leak under concurrent expire | test | Test verifies all semaphores are re-acquirable after expiration — catches semaphore leaks. |
</threat_model>

<verification>
1. `pytest tests/concurrent_tests.py -x -v -k test_expiration_releases_seats` passes
2. `pytest tests/test_phase2_e2e.py -x -v` still passes (no regressions)
3. `pytest tests/ -x --ignore=tests/concurrent_tests.py` passes (all non-concurrent tests)
</verification>

<success_criteria>
1. [ ] Concurrent expiration test exists in concurrent_tests.py
2. [ ] Test verifies seats released under concurrent RESERVE + expire
3. [ ] Test verifies semaphore counts restored
4. [ ] All seats AVAILABLE after test completes
5. [ ] All existing tests still pass
</success_criteria>

<output>
Create `.planning/phase-02-fix-expiration-restart-cleanup/02-02-SUMMARY.md` when done
</output>
