---
phase: 03-buy-near-expiry-cancellation
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tests/test_transaction_races.py
  - tests/test_phase3_e2e.py (NEW)
autonomous: true
requirements: [PCH-01, PCH-02, CNC-01, CNC-02]
user_setup: []
must_haves:
  truths:
    - "E2E tests confirm purchase-near-expiry completes without seat loss or semaphore corruption"
    - "E2E tests confirm concurrent cancellation releases seats correctly"
    - "Both race tests use real expire_session path (not legacy expire_reservation wrapper)"
    - "All existing tests pass after test additions"
  artifacts:
    - path: "tests/test_transaction_races.py"
      provides: "Updated race tests using real expire_session path"
      pattern: "monitor_thread.expire_session"
    - path: "tests/test_phase3_e2e.py"
      provides: "E2E tests for purchase near expiry and concurrent cancellation"
      min_lines: 80
  key_links:
    - from: "test_transaction_races"
      to: "MonitorThread.expire_session"
      via: "Use real expiration path instead of legacy expire_reservation wrapper"
---
<objective>
Write E2E tests that verify the race fix from Plan 03-01 works correctly:
1. Purchase-near-expiry (CONFIRM concurrent with real via-MonitorThread expiration)
2. Concurrent cancellation consistency (multiple threads cancelling overlapping sessions)
3. Semaphore integrity after concurrent cancel/expire (capacity matches available count)

Existing `test_transaction_races.py` uses `expire_reservation()` (legacy wrapper) — update to use real `expire_session` path via MonitorThread.
</objective>

<execution_context>
@/home/montoshita/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/montoshita/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phase-03-buy-near-expiry-cancellation/03-01-PLAN.md
@src/server/monitor_thread.py
@src/server/transactional_thread.py
@src/server/session_manager.py
@tests/test_transaction_races.py
@tests/concurrent_tests.py
@tests/test_phase1_e2e.py
@tests/test_phase2_e2e.py
@src/client/concert_client.py
@src/utils/enums.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update test_transaction_races.py — use real expire_session path</name>
  <files>tests/test_transaction_races.py</files>

  <read_first>
  @tests/test_transaction_races.py (full)
  @src/server/monitor_thread.py (for expire_session API)
  @src/server/session_manager.py (for session manipulation — get_by_session_id)
  @src/utils/enums.py (for Session — wait, session_manager uses dataclass)
  </read_first>

  <action>
  Update `test_confirm_vs_expire_keeps_consistency` and `test_cancel_vs_expire_releases_once` to use the real MonitorThread expiration path instead of the legacy `expire_reservation()` wrapper.

  **Current approach (legacy):**
  ```python
  results = _run_parallel(
      expire_fn=lambda: server.monitor_thread.expire_reservation(tx_id),
      client_fn=lambda: client.confirm(tx_id),
  )
  ```

  **New approach (real path via session expiration):**
  ```python
  def expire_through_monitor():
      # Get the session_id from the reserve response (already have tx_id)
      session = server.session_manager.get_by_session_id(tx_id)
      session.last_activity = time.time() - 600  # Push TTL expiry
      # Use a barrier to ensure MonitorThread polls after TTL is pushed
      time.sleep(1.5)  # Wait for MonitorThread poll cycle (1s poll + margin)

  results = _run_parallel(
      expire_fn=expire_through_monitor,
      client_fn=lambda: client.confirm(tx_id),
  )
  ```

  Actually, a cleaner approach: directly call `server.monitor_thread.expire_session()` instead of `expire_reservation()`. The `expire_reservation()` wrapper now delegates to `expire_session()`, but we want to test the real code path explicitly.

  ```
  results = _run_parallel(
      expire_fn=lambda: server.monitor_thread.expire_session(
          server.session_manager.get_by_session_id(tx_id)
      ),
      client_fn=lambda: client.confirm(tx_id),
  )
  ```

  This exercises the real `expire_session` with its `table_and_sections` lock acquisition, double-check, seat release, and semaphore release — not the legacy wrapper.

  Update both existing tests (`confirm_vs_expire` and `cancel_vs_expire`) using this pattern.

  Keep the same assertions as before:
  - CONFIRM wins: seat SOLD, semaphore = capacity - 1
  - EXPIRE wins: seat AVAILABLE, semaphore = capacity
  - No seat loss in either case

  Add `import time` at the top if not already imported.
  </action>

  <acceptance_criteria>
  1. `test_confirm_vs_expire_keeps_consistency` calls `server.monitor_thread.expire_session()` not `expire_reservation()`
  2. `test_cancel_vs_expire_releases_once` calls `server.monitor_thread.expire_session()` not `expire_reservation()`
  3. `pytest tests/test_transaction_races.py -x -v` passes all 2 tests
  4. Tests verify seat state and semaphore count are consistent in both outcomes
  </acceptance_criteria>

  <verify>
  <automated>pytest tests/test_transaction_races.py -x -v 2>&1 | tail -15</automated>
  </verify>

  <done>Race tests now use real expire_session path — covers lock acquisition, double-check, seat release, and semaphore release.</done>
</task>

<task type="auto">
  <name>Task 2: Create test_phase3_e2e.py — purchase-near-expiry E2E tests</name>
  <files>tests/test_phase3_e2e.py (NEW)</files>

  <read_first>
  @tests/test_phase1_e2e.py (fixture pattern)
  @tests/test_phase2_e2e.py (fixture pattern)
  @src/server/monitor_thread.py (for expire_session)
  @src/server/session_manager.py (for get_by_session_id)
  @src/client/concert_client.py (ConcertClient API)
  </read_first>

  <action>
  Create `tests/test_phase3_e2e.py` with E2E tests for purchase-near-expiry scenarios.

  **A) `test_confirm_succeeds_when_expire_runs_first`:**
  - Start server on random port
  - Client A reserves VIP(0,5) → gets session_id
  - Call `server.monitor_thread.expire_session(session)` directly (simulate MonitorThread acting first)
  - Client A tries CONFIRM → should fail (TransactionNotActiveError)
  - Verify: seat is AVAILABLE, semaphore count = full capacity
  - This verifies the double-check in handle_confirm works (session expired → CONFIRM gracefully fails)

  **B) `test_expire_skips_when_confirm_wins`:**
  - Start server on random port
  - Client A reserves VIP(0,6) → gets session_id
  - In parallel:
    - Thread 1: CONFIRM
    - Thread 2: expire_session
  - Wait for both
  - Verify invariant: either CONFIRM won (seat SOLD) or expire won (seat AVAILABLE)
  - Never: seat RESERVED, or seat lost (semaphore mismatch), or double release
  - Use barrier for synchronized launch

  **C) `test_consecutive_confirm_expire_rounds`:**
  - Run 5 rounds of: reserve → (confirm vs expire race) → verify
  - Each round uses a fresh session and seat
  - After all rounds: verify total seat counts match, no semaphore leaks
  - This is a short iteration test (< 5 seconds)

  Use the `_wait_for_server` helper pattern or `time.sleep(0.5)` for server readiness.
  Use random ports to avoid conflicts.
  Use socket bind pattern from `test_transaction_races.py`.
  </action>

  <acceptance_criteria>
  1. `pytest tests/test_phase3_e2e.py --collect-only` shows 3 tests collected
  2. `test_confirm_succeeds_when_expire_runs_first` passes (CONFIRM gracefully fails after expire)
  3. `test_expire_skips_when_confirm_wins` passes (consistent state in either outcome)
  4. `test_consecutive_confirm_expire_rounds` passes (5 rounds, no seat loss)
  5. Semaphore counts match available seats after all tests
  </acceptance_criteria>

  <verify>
  <automated>pytest tests/test_phase3_e2e.py -x -v 2>&1 | tail -20</automated>
  </verify>

  <done>3 E2E tests cover purchase-near-expiry: CONFIRM after expire, parallel CONFIRM vs expire, multi-round consistency.</done>
</task>

<task type="auto">
  <name>Task 3: Add concurrent cancellation E2E tests to test_phase3_e2e.py</name>
  <files>tests/test_phase3_e2e.py</files>

  <read_first>
  @tests/test_phase3_e2e.py (just created in Task 2 — append to it)
  @src/client/concert_client.py (cancel API)
  @src/utils/enums.py (SeatState, Section, ReservationStatus)
  @src/shared_resources/seat_matrix.py (for seat state checks)
  </read_first>

  <action>
  Append the following TEST FUNCTIONS (not replacing existing ones) to `tests/test_phase3_e2e.py`:

  **D) `test_cancel_vs_cancel_same_session`** — Two concurrent CANCELs for the same session:
  - Reserve VIP(0,7) → get session_id
  - Launch 2 threads both calling `client.cancel(tx_id)` with barrier sync
  - First CANCEL wins (session → CANCELLED, seats AVAILABLE)
  - Second CANCEL fails (TransactionNotActiveError)
  - Verify: seat is AVAILABLE, semaphore = full capacity, exactly one CANCEL succeeded
  - This tests the double-check in handle_cancel with concurrent CANCEL

  **E) `test_cancel_vs_reserve_overlapping_seat`** — CANCEL session A while RESERVE targets same seat:
  - Client A reserves VIP(0,8) → session A, seat VIP(0,8)
  - Client B starts fresh (no session yet)
  - In parallel (barrier sync):
    - Thread A: CANCEL session A (releases VIP(0,8))
    - Thread B: RESERVE VIP(0,8)
  - Verify: one of these happened:
    - CANCEL won + RESERVE won: seat AVAILABLE → RESERVED (by B). Seat belongs to B's session.
    - CANCEL won but RESERVE failed on semaphore (seat AVAILABLE but semaphore not yet released — the timing window this phase targets). Actually with the fixes in 03-01, semaphore is released inside the lock, so this should never happen. The test exists to prove the fix works.
    - RESERVE won first → then CANCEL releases the seat (CANCEL runs, session A has VIP(0,8), marks it AVAILABLE). But wait — if RESERVE won first, the seat is RESERVED, then CANCEL would see seat RESERVED (session A still has VIP(0,8) in its seat list) and mark it AVAILABLE. But that's wrong — RESERVE (client B) just reserved it!
    
    Actually this is the KEY race the plan targets. Let me reconsider:
    
    Scenario: Session A has seat VIP(0,8) in its seats list. Client B tries to RESERVE VIP(0,8).
    - The RESERVE checks: is VIP(0,8) AVAILABLE? It's RESERVED (by session A) → fails immediately. So RESERVE never succeeds if the seat is still reserved.
    
    So the CANCEL + RESERVE scenario needs different seats:
    - Client A reserves VIP(0,8) AND VIP(0,9) (two seats in same session section)
    - Client B tries to RESERVE VIP(0,9) (same seat)
    - In parallel: A cancels (releases both VIP(0,8) and VIP(0,9)), B tries to reserve VIP(0,9)
    - Outcome: either B gets VIP(0,9) (if it picks it up after cancel releases it) or B fails (if it tries before cancel completes)
    - Verify: no double-booking, no seat loss

  **Simplify the test:** Use a straightforward concurrent cancel + reserve pattern where the cancel frees up a seat that the reserve targets. Use enough threads and iterations to exercise the timing.

  **F) `test_semaphore_integrity_after_concurrent_cancel`** — Stress concurrent cancels:
  - Reserve N seats (N clients, 1 seat each, spread across 2 sections)
  - Launch N/2 cancel threads concurrently
  - After all done: verify semaphore count == available seats for each section
  - No SOLD seats should exist (no confirms were issued)
  - This catches semaphore leaks or double-releases
  </action>

  <acceptance_criteria>
  1. `pytest tests/test_phase3_e2e.py --collect-only` shows 6 tests collected
  2. `test_cancel_vs_cancel_same_session` passes (exactly one cancel succeeds)
  3. `test_cancel_vs_reserve_overlapping_seat` passes (no double-booking, no seat loss)
  4. `test_semaphore_integrity_after_concurrent_cancel` passes (semaphore == available count)
  5. All tests complete in < 30 seconds combined
  </acceptance_criteria>

  <verify>
  <automated>pytest tests/test_phase3_e2e.py -x -v 2>&1 | tail -25</automated>
  </verify>

  <done>6 E2E tests cover concurrent cancellation: same-session cancel, cancel vs reserve overlap, semaphore integrity stress test.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-S1 | Tampering | test_transaction_races legacy wrapper | test | Update to use real expire_session path — covers actual code path used in production |
| T-03-S2 | Denial of Service | Semaphore leak not caught by existing tests | test | Semaphore integrity stress test catches over/under-release under concurrent load |
| T-03-S3 | Spoofing | Missing coverage for CONFIRM after concurrent CANCEL | test | E2E test verifies seat state and semaphore count in both CANCEL-first and CONFIRM-first outcomes |
</threat_model>

<verification>
1. `pytest tests/test_transaction_races.py -x -v` passes (2 tests using real expire_session)
2. `pytest tests/test_phase3_e2e.py -x -v` passes (6 new E2E tests)
3. `pytest tests/ -x --ignore=tests/concurrent_tests.py --ignore=tests/test_phase3_e2e.py` passes (all existing tests)
4. `python -c "from src.server.monitor_thread import MonitorThread; from src.server.transactional_thread import TransactionalThread; print('OK')"` passes (no import errors)
</verification>

<success_criteria>
1. [ ] `test_transaction_races.py` uses `expire_session()` not `expire_reservation()`
2. [ ] `test_phase3_e2e.py` has 6 tests: confirm-near-expiry (3) + concurrent cancel (3)
3. [ ] `test_confirm_succeeds_when_expire_runs_first` — CONFIRM gracefully fails after expire
4. [ ] `test_expire_skips_when_confirm_wins` — consistent state in either race outcome
5. [ ] `test_consecutive_confirm_expire_rounds` — 5 rounds, no seat loss
6. [ ] `test_cancel_vs_cancel_same_session` — exactly one cancel succeeds
7. [ ] `test_cancel_vs_reserve_overlapping_seat` — no double-booking or seat loss
8. [ ] `test_semaphore_integrity_after_concurrent_cancel` — semaphore == available count
9. [ ] All existing standard tests still pass
</success_criteria>

<output>
Create `.planning/phase-03-buy-near-expiry-cancellation/03-02-SUMMARY.md` when done
</output>
